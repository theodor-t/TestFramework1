package pages.am_catalogs;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class Regions extends BasePage {
    public Regions(WebDriver driver) {
        super(driver);
    }
}
