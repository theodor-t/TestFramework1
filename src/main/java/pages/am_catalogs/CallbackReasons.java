package pages.am_catalogs;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class CallbackReasons extends BasePage {
    public CallbackReasons(WebDriver driver) {
        super(driver);
    }
}
