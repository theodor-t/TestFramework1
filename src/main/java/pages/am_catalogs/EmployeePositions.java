package pages.am_catalogs;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class EmployeePositions extends BasePage {
    public EmployeePositions(WebDriver driver) {
        super(driver);
    }
}
